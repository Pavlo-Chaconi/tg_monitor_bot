services:
  bot:
    build: .
    container_name: tg_monitor_bot
    restart: unless-stopped
    env_file: .env
    # bridge-сеть по умолчанию: работает на Windows, macOS и Linux
    # Prometheus и TrueNAS задаются через IP в .env — контейнер достучится по ним через NAT
